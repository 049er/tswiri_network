name: tswiri_network
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  path: ^1.8.0
  shelf_web_socket: ^1.0.3
  shelf: ^1.2.0
  shelf_router: ^1.0.0
  shelf_static: ^1.0.0

  tswiri_database:
    git:
      url: https://github.com/TswiriSoftware/tswiri_database.git
      ref: isar_update # branch name

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
